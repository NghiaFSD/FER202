import React,{useEffect} from 'react';import {useDispatch,useSelector} from 'react-redux';import {loadProducts} from './store';
export default function ProductList(){const dispatch=useDispatch();const {products,loading,error}=useSelector(s=>s.shop);useEffect(()=>{dispatch(loadProducts())},[dispatch]);return <div><h2>Products</h2>{loading&&<p>Loading...</p>}{error&&<p className="text-danger">{error}</p>}{products.map(p=><div className="card p-3 mb-2" key={p.id}><h5>{p.title}</h5><p>${p.price}</p><button className="btn btn-success" onClick={()=>dispatch({type:'ADD_TO_CART',payload:p})}>Add to Cart</button></div>)}</div>}
